version: '3'

services:

  web:
    build:
        context: ./
        dockerfile: ./docker/flask/Dockerfile
    ports:
        - "5000:5000"
    depends_on:
        - mysql
    volumes:
        - "./app:/var/www/flask/app"

  redis:
    image: "redis:alpine"